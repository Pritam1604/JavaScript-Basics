// A program to identify which is odd and which is even.

const numTest = prompt("Enter a number of your Choice: ");
if(numTest%2===0){
    console.log(`The number is even. The number is ${numTest} `);
} else{
    console.log(`The number is odd. The number is ${numTest} `);
}

